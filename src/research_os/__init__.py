"""Source-governed research and learning OS.

Stage 2 vertical slice: one mission -> one connector -> canonicalize + dedupe
-> classify by source class -> rerank (class before relevance) -> SQLite memory
-> one briefing. See docs/operations/implementation-order.md.
"""

__version__ = "0.1.0"
